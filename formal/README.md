# Formal proofs (SymbiYosys)

Machine-checked proofs of the core's timing contract: `pe_core.v` (with `pe_gpio.v` and
`pe_timebase.v`) under `src/isa_defs.vh`. Properties live in the RTL itself, inside
`` `ifdef FORMAL `` blocks (see `src/pe_core.v` and `src/pe_gpio.v`, near `endmodule`), so they
cannot affect synthesis, simulation or lint -- `read_verilog -formal` is what defines `FORMAL`;
every other flow (Verilator lint, cocotb, LibreLane) never defines it.

## Setup

```
source ../env.sh    # OSS CAD Suite: sby, yosys, yices-smt2, boolector, z3, bitwuzla
cd formal
sby -f core.sby     # runs all five tasks below: bmc, prove, cover, timebase_bmc, timebase_prove
```

`core.sby` is a single file with two groups of tasks, selected by an implicit self-tag per task
plus two shared tags (`core` for `bmc`/`prove`/`cover`, `tb` for `timebase_bmc`/`timebase_prove`)
used to give each group its own `[script]`/`[files]`/`[engines]` content -- see the `[tasks]`
section. `sby -f core.sby` with no task names runs all five (SymbiYosys's default task list is
"every task" unless a `default` tag restricts it, which this file does not use).

Logs land in `formal/core_bmc/`, `formal/core_prove/`, `formal/core_cover/`,
`formal/core_timebase_bmc/`, `formal/core_timebase_prove/` (one directory per task, named
`<jobname>_<taskname>` since there is more than one task in the file; `logfile.txt` has the full
engine transcript, `engine_0/trace*.vcd` / `trace*.yw` hold any counterexample or cover witness).
These directories are gitignored and regenerated by `sby -f`.

`formal/core_props.v` is the `core`-tagged proof harness: it instantiates `pe_timebase`,
`pe_gpio` and `pe_core` with the instruction stream, `ui_in`/`uio_in`, `prescale` and the
host-side signals (`h2c_valid`, `h2c_data`, `c2h_full`) all free (`(* anyseq *)` -- an arbitrary
value every cycle), and `rst_n` held low for cycle 0 only, then permanently high. This is
deliberately more permissive than the real chip in most respects (e.g. `imem_data` is free every
cycle rather than a function of `imem_addr`) -- any property proved here also holds for every
more-constrained real environment. **One deliberate exception**: the timebase's `en` is tied to
the constant `1'b1`, exactly as `src/project.v` (the real top level) wires it, *not* to the free
`run` signal. Tying `en` to `run` instead (as an earlier version of this harness did) would have
been a *restriction* relative to the chip, not a relaxation -- the real timebase always counts,
independent of RUN -- so a property proved only under that restriction would not automatically
carry over to the real, more-permissive chip. `en` is therefore wired chip-accurately.

`formal/timebase_props.v` is the `tb`-tagged proof harness for T4's "counting part" (see below);
it additionally fixes `prescale` to an arbitrary-but-constant value (`(* anyconst *)`), which is
the one place this proof set makes an assumption beyond the reset sequence -- see "What's
assumed".

## Theorems

- **T1_deadline_exact** (`src/pe_core.v`): whenever the core is `active` on a `WAITT`
  instruction, `adv == (t_in == rt_v)` -- it releases in exactly the cycle `T == Rn`, never
  earlier, never later.
- **T1b** (`src/pe_core.v`, spec-pinning restatement): `rt_v == (w_rt == 0) ? 0 : regs[w_rt]` --
  ties T1's `rt_v` to the actual register file / source-register select, so a bug in *that*
  selection (as opposed to the deadline comparison itself) is covered by some property too.
- **T2_static_timing** (`src/pe_core.v`, two parts): (a) whenever `active` and the instruction
  is neither a `WAIT` (any sub-op) nor `HALT`, `adv` is 1 -- it completes in the cycle it is
  active, no hidden multi-cycle stalls. (b) whenever `adv` was 1 last cycle, `pc` now equals last
  cycle's `next_pc` -- a completed instruction's chosen successor address (including a taken
  redirect) is really what lands in `pc`.
- **T3_reset_safety, grant mask** (`src/pe_gpio.v`): `(uio_oe & ~f_oe_granted) == 0` in every
  cycle, where `f_oe_granted` accumulates `wr_mask` on every executed OE-*set* write
  (`wr_en && wr_op==ISA_PIN_OE && wr_bank`) and is cleared only on reset -- `uio_oe` may only ever
  have bits on that were actually granted by firmware, from reset onward (this replaces the
  original one-shot "has any OE write ever happened" formulation -- see "Mutation testing").
- **T3b/T3c** (`src/pe_gpio.v`, spec-pinning restatements): the exact next-state of `uio_oe`
  following an executed OE write -- an OE-clear write with mask `m` leaves those bits 0 the next
  cycle (T3b); an OE-set write with mask `m` leaves them 1 the next cycle (T3c).
- **T4_bounded_wait** (`src/pe_core.v`, structural; `formal/timebase_props.v`, folded into
  `core.sby`'s `timebase_bmc`/`timebase_prove` tasks): a timed wait (`WAITP`/`WAITF`/`WAITL`,
  i.e. any `WAIT` sub-op other than `WAITT`/`DELAY`) cannot stall more than 65536 ticks. Proved
  as two structural lemmas in `pe_core.v`: (i) once `wait_active` latches for a wait that does not
  complete this cycle, its deadline `wdead` is held constant until the wait completes; (ii) the
  wait completes (`adv`) in any cycle where `t_in == wdead`. See "T4's bound: what's proved vs.
  argued" below for how this becomes the 65536-tick bound, and what the folded-in counting-part
  tasks add.
- **T5_side_effects_only_when_active** (`src/pe_core.v` T5a/T5b/RUN_LINK,
  `formal/core_props.v` T5c/T5d): `gpio_wr_en`, `gpio_pin_en`, `h2c_pop`, `c2h_push` (T5b), the
  GPIO output registers `uo_out`/`uio_out`/`uio_oe` (T5c), and the host FIFO strobes as seen from
  the wrapper (T5d) are all quiescent whenever `active` is/was 0 (covers halted, RUN low, and the
  `core_reset` cycle). **T5a** independently re-derives `active`'s formula
  (`run_q && !halted && !core_reset`, the ISA's own `semantics.run` contract). **RUN_LINK**
  additionally proves `run_q` itself genuinely tracks `run` delayed one cycle (with the RTL's
  actual reset override). See "What is pinned vs. independent" and "Mutation testing" for why
  RUN_LINK exists as a *separate* property from T5a.

## What is pinned vs. independent

Some of the properties above are **independent facts that hold over time / across a boundary**;
others are **spec-pinning restatements** of a single RTL continuous-assignment or `<=` line,
kept even though they are tautological *given the current RTL* because mutation testing shows
each one is killed by a specific class of regression that the independent properties around it
do not catch.

| Property | Kind | Why kept |
|---|---|---|
| T1 | independent (2-cycle relation between `adv`, `t_in`, `rt_v`) | the deadline-exactness contract itself |
| T1b | **spec-pinning** (`rt_v` == its own defining expression) | killed by a bug in the source-register select feeding `rt_v` |
| T2a | independent | the "every non-WAIT instruction completes in 1 cycle" contract |
| T2b | independent (relates `pc` to `$past(next_pc)`, which itself depends on `redir_v`/`redir_t`, i.e. state carried from an earlier cycle, not just this cycle's inputs) | catches a broken redirect path (see mutation M5) |
| T3 (grant mask) | independent (`f_oe_granted` is an accumulator over history, not a restatement of any single line) | the actual reset-safety contract |
| T3b/T3c | **spec-pinning** (`uio_oe`'s own next-state expression, restated) | killed directly by an OE write producing the wrong next-state (mutation M4) |
| T4 part 1 | independent (`wdead` stability is a multi-cycle invariant, not a single-line restatement) | catches deadline re-capture bugs (mutation M6) |
| T4 part 2 | **spec-pinning** (`wait_done`'s timed-completion case, restated as "adv when t_in==wdead") | killed by a broken timeout-detection path |
| T5a | **spec-pinning** (`active`'s own continuous assignment, restated verbatim) | killed by a bug in `active`'s formula itself (mutation M2), but *not* by a bug in `run_q`'s own update (see RUN_LINK) |
| RUN_LINK | independent (relates `run_q` to `$past(run)`, i.e. a genuine one-cycle-delay contract) | killed by mutation M3, which T5a alone cannot see |
| T5b | independent (relates 4 strobes to `active`, a different signal) | the strobe-quiescence contract |
| T5c/T5d | independent (cross-module: needs pe_gpio's own register-hold behaviour, not just pe_core's strobes) | closes the "nothing changes" half at the actual chip outputs, not just at pe_core's boundary |

The spec-pinning properties are not redundant busywork: each one is the difference between a
mutant being caught immediately (`bmc` fails at step 0-3) and passing silently. They are simply a
different *kind* of guarantee than the independent properties -- "this one RTL line still says
what it always said" rather than "this behavioural contract holds for all time".

## What's assumed

The *only* assumption in `core.sby`'s `core`-tagged tasks (`bmc`/`prove`/`cover`) /
`core_props.v` is the reset sequence built into the wrapper: `rst_n` is low for cycle 0, then
permanently high (never asserted again). Everything else -- the instruction stream, all chip
inputs, all host-interface signals -- is free every cycle. No `assume` statements appear anywhere
in `src/pe_core.v` or `src/pe_gpio.v`; every property beyond the brief's original T1-T3 is an
`assert` (a proven lemma), never a weakened hypothesis.

The `tb`-tagged tasks (`timebase_bmc`/`timebase_prove`) / `formal/timebase_props.v` (T4's
"counting part", see below) additionally assume `en` is tied to constant 1 (this is *not* an
extra assumption relative to the real chip, since `en` is chip-accurately wired to `1'b1` in
`core_props.v` too now -- see "Setup") and that `prescale` is fixed to an arbitrary-but-constant
value (`(* anyconst *)`) for the whole trace -- this second one *is* the one place this proof set
permits an extra assumption beyond reset ("the timebase configuration", as the task brief allows).
It does not weaken T1-T5's own proofs, which remain assumption-free beyond reset.

## T4's bound: what's proved vs. argued

The 65536-tick bound is the composition of three facts:

1. **(proved, `pe_core.v`, `core.sby`'s `core`-tagged tasks)** While a timed wait is outstanding
   and hasn't completed, its latched deadline `wdead` never changes.
2. **(proved, `pe_core.v`, `core.sby`'s `core`-tagged tasks)** The wait completes (`adv`) in any
   cycle where `t_in == wdead`.
3. **(proved, `formal/timebase_props.v`, `core.sby`'s `tb`-tagged tasks)** T (`t_out`) never
   skips a value -- each cycle it either holds or advances by exactly 1 -- and the timebase's
   internal tick period never exceeds the fixed `prescale`, so ticks cannot stall forever.

Facts 1+2 give: if T ever again equals the frozen `wdead`, the wait completes that cycle, and
that value can never be skipped over (fact 3's "no skip"). Since T is a free-running 16-bit
counter, it must revisit every value (in particular `wdead`) within one 65536-tick wrap. Fact
3's "ticks cannot stall forever" additionally rules out T freezing forever before completing that
wrap.

**The composed bound is an argument, not a single machine-checked property**, and it carries
hypotheses beyond what facts 1-3 alone state:

- **The core must stay `active` for the whole wait.** If `run` is held low (or `core_reset`
  asserted) partway through, `wait_active`/`wdead` freeze (they are core state, not timebase
  state) and the wait simply does not progress -- this is a real, uninteresting way to violate a
  liveness claim that has nothing to do with the core's own correctness, and it is why fact 3's
  proof needs its own `en == 1` (unconditional counting) assumption rather than inheriting the
  main proof's free `run`.
- **`prescale` must not be reprogrammed while the wait is outstanding.** Fact 3 is proved for a
  single fixed `prescale` (`(* anyconst *)`); if firmware changes `prescale` mid-wait the tick
  period changes too, and the "revisits every value within 65536 ticks" argument (which relies on
  T always advancing by exactly 1, never skipping -- true regardless of `prescale`'s value, but
  the *periodicity* bound in fact 3 is specific to whichever `prescale` was fixed for that
  particular induction run) no longer composes with a single period bound.

Because both extra hypotheses are about the *environment* during the wait rather than about the
RTL itself, and because SymbiYosys's BMC/k-induction prove safety invariants, not
liveness/eventuality directly, the full bound is stated here as an argument built from three
independently machine-checked, unbounded (k-induction) facts, not as a single end-to-end formal
property. A `mode live` proof with fairness constraints on `en` and `prescale` would be needed to
turn this into one property, which was judged out of scope.

## What is NOT proved

- **The RTL is not proved equivalent to `tools/sim.py` or `isa/isa.yaml`.** These SymbiYosys
  proofs are entirely about `pe_core.v`/`pe_gpio.v`/`pe_timebase.v` as a closed system against the
  timing/reset-safety properties above; they say nothing about whether the RTL implements the ISA
  spec's *semantics* (register/ALU/branch behaviour, opcode encodings, etc.) correctly. That
  equivalence is checked empirically, not formally, by the cycle-accurate differential fuzzer in
  `test/test_diff.py` (`Makefile.core`), which runs both the RTL (via cocotb) and `tools/sim.py`
  against the same randomized instruction streams and compares state every cycle.
- **The 65536-tick bound is bounded-hypothesis, not unconditional** -- see "T4's bound: what's
  proved vs. argued" immediately above: it additionally requires the core to stay active and
  `prescale` to stay fixed for the duration of the wait, and it is an argument composed from
  separately-proved facts, not one machine-checked property.
- **The proof harness's instruction stream is looser than real memory.** `imem_data` is free
  every cycle in `core_props.v`, not a deterministic function of `imem_addr` -- the real
  `pe_imem_ff` always returns the same word for the same address. This makes every property
  proved here strictly *stronger* (true for every possible memory contents, including
  pathological ones), but it also means a cover witness can show the *same pc* decoding as
  different instruction words across consecutive cycles (see "Cover witnesses" below for two
  concrete examples of this in the WAITT and timeout witnesses) -- this is a known, deliberate
  looseness of the harness, not a defect in the properties.
- **No gate-level / synthesis / place-and-route equivalence is checked here.** These proofs run
  on the pre-synthesis RTL only.

## Mutation testing

All mutations were applied and tested in a scratch copy of the repository (`git worktree add`),
never on the working tree used for the final commit; the working tree was confirmed unmodified
(`git status`/`git diff --stat` clean) throughout. See the task report for full transcripts.

| # | Mutation | Expected to fail | Result |
|---|---|---|---|
| M1 | `waitt_done = (t_in == rt_v + 16'd1)` (WAITT release off-by-one) | T1 | **FAILS** `bmc` at step 3 (T1_deadline_exact, `pe_core.v` T1 assert) |
| M2 | drop `!core_reset` from `active`'s assignment | T5 (T5a) | **FAILS** `bmc` at step 1 (T5a) |
| M3 | `run_q <= 1'b1;` (RUN ignored) | RUN_LINK (new) | **FAILS** `bmc` (RUN_LINK `run_q == $past(run)` / `run_q == 0` assert); confirmed T5a alone does **not** fail |
| M4 | OE write does `uio_oe <= 8'hFF;` unconditionally | T3 (grant mask) | **FAILS** `bmc` (T3 grant-mask assert, and also T3b/T3c) |
| M5 | `pc <= pc_p1;` (redirect ignored) | T2b | **FAILS** `bmc` (T2b `pc == $past(next_pc)` assert) |
| M6 | `wdead` re-captured every cycle regardless of `wait_active` | T4 part 1 | **FAILS** `bmc` (T4 part-1 `wdead == $past(wdead)` assert) -- see note below |
| M7 | `gpio_wr_en` for the PIN class permanently 0 | some cover goal | **FAILS** `cover` (the post-reset OE-executes cover in `pe_core.v` and the OE grant-mask cover in `pe_gpio.v` both become unreachable) |

The unmutated design passes every one of the five `core.sby` tasks, and `core_prove` /
`core_timebase_prove` both report `successful proof by k-induction.` in their logs (not just
`DONE (PASS, rc=0)` -- see "Results" below for exact log lines).

Two mutations from the original submission's write-up are re-confirmed still relevant here:

- **T1**: `bmc` (depth 20, boolector) fails at step 3, `assert(adv == (t_in == rt_v))` violated
  with `rt_v == 0`, mutant releasing at `t_in == 1` instead.
- **T5 / active's own formula (M2)**: with only the brief's originally-specified T5 (T5b) in
  place, this mutation passes silently (T5b is structurally a tautology through `active`, however
  `active` is defined, since the four strobes are all `active && ...`). T5a
  (`assert(active == (run_q && !halted && !core_reset))`) is what actually catches it.

**A note on M6's exact implementation.** The literal text `wdead <= dead_now;` unconditionally
(i.e. just moving that assignment out of the `if (!wait_active)` guard, keeping `dead_now`'s own
`wait_active ? wdead : (t_in+tmo)` ternary unchanged) turns out to be a **no-op**: non-blocking
assignment semantics mean `wait_active` on `dead_now`'s right-hand side always reads the
*pre-edge* value, so on every cycle where a wait is already outstanding (`wait_active==1` before
the edge), `dead_now` evaluates to the current `wdead` itself, making `wdead <= wdead` a true
identity -- confirmed empirically (this exact literal mutation was applied and `bmc` passed
cleanly through the full depth). The actually-buggy re-capture this mutation is meant to model was
therefore implemented as `wdead <= t_in + tmo;` unconditionally (recomputing the deadline fresh
from the current `t_in`/`tmo` every cycle rather than the latch degenerating to itself) -- this is
what fails T4 part 1 as shown in the table above.

And the review that produced this round of fixes additionally found: **T5a does not catch M3**
(`run_q <= 1'b1;`), because that mutation never touches the `assign active = ...` line T5a
re-types -- T5a re-derives `active` purely in terms of `run_q`/`halted`/`core_reset` as *inputs*,
without checking that `run_q` itself has the right value. RUN_LINK closes this by directly
constraining `run_q` against `$past(run)`.

## Cover witnesses

All 5 cover goals are gated on `rst_n && f_past_valid` (and `$past(rst_n)` where a `$past` is
used), so none of them can be witnessed by garbage pre-reset flop state -- two of the original
five covers (the timed-wait-timeout cover and the OE-executes cover) had no such guard at all
before this round of fixes, and were reachable using uninitialised-register garbage at cycle 0
without ever going through a real reset.

Two witnesses were decoded from their VCDs (`formal/core_timebase_*` are unrelated to cover;
witnesses live under `formal/core_cover/engine_0/`) to confirm they are genuine and not an
artifact of the free-`imem_data` harness silently reusing a different instruction:

- **WAITT release** (`pe_core.v:327-328`, reached at step 6, `engine_0/trace4.vcd`): cycle 2
  executes an LDI (pc 0 -> 1). Cycle 3 decodes a WAIT-class instruction at pc 1 that does not
  complete (`adv=0`); because `wait_active` was 0 going in, this cycle's decode (a `DELAY`,
  `w_sub=1`, in this particular witness) is what actually latches `wait_active<=1` and
  `wdead<=t_in+tmo` (both 0, since `w_rt` happened to select a still-zero register). Cycle 4, same
  pc 1 (since cycle 3 didn't advance), now decodes as `WAITT` (`w_sub=0`) with `wait_active=1`
  (carried over) and `rt_v=4 != t_in(0)` -- still stalling (`adv=0`); this is the cycle the
  auxiliary `f_waitt_stall_seen`/`f_waitt_stall_pc` latch (pc=1). Cycle 5, same pc 1 again, decodes
  `WAITT` once more, this time with `rt_v=0 == t_in(0)` -- releases (`adv=1`). The cover fires
  here: `f_waitt_stall_seen && pc==f_waitt_stall_pc(1) && is_wait && w_sub==WAITT && adv &&
  t_in==rt_v`. This is a genuine T1 release witness (deadline met exactly, at the recorded pc),
  though -- exactly as flagged in "What is NOT proved" -- the *instruction word* backing the
  stall (`DELAY`) differs from the word backing the eventual `WAITT` release, a direct consequence
  of `imem_data` being free every cycle rather than fixed per address. This is what the fix
  actually rules out compared to the old cover (which required no `is_wait`/`w_sub` match at the
  release cycle *at all*): the release is now provably a `WAITT` completing exactly at its
  deadline at the *same address* the stall was observed at, not an arbitrary unrelated advancing
  instruction.
- **Timed-wait timeout** (`pe_core.v:349-351`, reached at step 6, `engine_0/trace3.vcd`): cycle 2
  decodes a `WAITF` at pc 0 with `w_rt=6` (a still-zero register, so `tmo=0`); since
  `wait_active=0` at decode time, `dead_now=t_in+tmo=0`, which already equals `t_in(0)` --
  `timed_to=1` the same cycle, `timed_cond=0` (flag false) -> `adv=1` immediately (a degenerate
  zero-length wait) and `fto<=1`. Cycle 3 (pc 1) decodes a `DELAY` that stalls (`adv=0`); since
  `wait_active` is 0 going in, this is what latches `wait_active<=1`, `wdead<=0` (again `w_rt`
  selects a zero register). Cycle 4, same pc 1, decodes `WAITF` again with `wait_active=1`
  (carried over) -- `dead_now=wdead=0==t_in(0)`, `timed_to=1`, `timed_cond=0` -> completes
  (`adv=1`) via genuine timeout with the flag/condition false; `f_timed_wait_started` is 1 at this
  point (latched back at cycle 2's decode, never cleared since `wait_active&&adv` never coincided
  before now) confirming a real post-reset start. Cycle 5: `fto=1`, satisfying the cover. As with
  the WAITT witness, `wait_active`/`wdead` (genuine registered state) persist correctly across
  cycles where the *combinationally re-decoded* instruction word at the same pc changes
  (`DELAY` -> `WAITF`), again a harness artifact disclosed in "What is NOT proved", not a defect
  in the fix.
- **Taken branch -> delay slot -> target** (`pe_core.v:378`, reached at step 5): the 3-stage
  `f_branch_stage` tracker reaches stage 2 with `pc == f_branch_target`, i.e. a taken redirect's
  delay-slot instruction actually executed and PC genuinely landed on the recorded target -- not
  merely "some instruction advanced next", which is what the previous 2-step cover only checked.
- **OE pin instruction executes** (`pe_core.v:386`, reached at step 3) and **OE grant-mask cover**
  (`pe_gpio.v:108`, reached at step 4): a post-reset OE-set write executes and `uio_oe` reads back
  exactly the accumulated grant mask the following cycle.

## Results

`core.sby`'s `core`-tagged tasks (`bmc`/`prove`/`cover`) use `smtbmc boolector` (yices-smt2 was
tried first and did not finish `bmc`/`prove` within 5 minutes at any reasonable depth on this
properties/design size; boolector closes the same properties in seconds). The `tb`-tagged tasks
(`timebase_bmc`/`timebase_prove`) use boolector for `bmc` and yices for `prove` (boolector's
k-induction countdown search stalled past 9 minutes on the 300-depth, 8-bit-counter induction
search where yices closes in ~37s). Depth and wall-clock time (`sby`'s own "Elapsed clock time")
per task, from the final clean run (`git status`/`git diff --stat` showing only the intended
file changes):

| Task | Mode | Engine | Depth | Result | Wall time | Notes |
|---|---|---|---|---|---|---|
| `bmc`             | BMC         | boolector | 20        | PASS              | 5s  | no assertion violated through step 19 |
| `prove`           | k-induction | boolector | 8 (bound) | PASS              | <1s | induction closed at step 5 |
| `cover`           | cover       | boolector | 20 (bound)| PASS (5/5 goals)  | <1s | all 5 goals reached by step 6 |
| `timebase_bmc`    | BMC         | boolector | 40 (bound)| PASS              | <1s | no assertion violated through step 39 |
| `timebase_prove`  | k-induction | yices     | 300 (bound)| PASS            | 36-37s | induction closed at step 43 |

`sby -f core.sby` (all five tasks, run in parallel by `sby`'s own scheduler) completes in
well under a minute of wall-clock time in total.

"Depth" for `prove` is the search bound sby was given (`prove: depth N`); k-induction itself
reports the actual step at which the induction check succeeded, which is what makes each of
these an *unbounded* proof (valid for all step counts, not just up to the bound) rather than a
bounded one -- noted in "Notes" above, and confirmed by the exact log line
`summary: successful proof by k-induction.` in `core_prove/logfile.txt` and
`core_timebase_prove/logfile.txt`. `bmc` depth is a true bound: it is a proof of no
counterexample within that many steps, not an unbounded proof, and is reported as such.

`cover` reached all 5 cover goals -- see "Cover witnesses" above for what each one was decoded to
show.

### Solver notes

The properties in `core_props.v` are all one- or two-cycle local relations (T1/T2/T4/T5 relate
`active`/`adv` in the current cycle, or the current cycle to `$past(...)`; T3 is a single-cycle
invariant over an accumulator) over a fully free (`anyseq`) instruction stream, so k-induction
closes at a small depth (step 5). In practice `yices-smt2` took over 5 minutes and had not
finished `bmc`/`prove` even at step ~22 out of 40/24 in an earlier attempt at an earlier version
of this property set; switching the engine to `boolector` and lowering the configured depths to
the guidance-recommended range (`bmc` 20, `prove` 8) brought both tasks under 11s combined. The
opposite held for `formal/timebase_props.v`'s `prove` task: its induction search (300-depth
bound, `since_tick <= prescale` over an 8-bit counter) ran past 9 minutes under boolector without
finishing and was killed, while `yices-smt2` closes it in ~37s (induction succeeds at step 43).
`core.sby`'s `[engines]` section therefore picks the engine per task-tag (`core: smtbmc
boolector`, `timebase_bmc: smtbmc boolector`, `timebase_prove: smtbmc yices`).

(`timebase_prove`'s bound is 300, not 8: an 8-bit counter admits k-induction starting states past
`prescale` that need up to a full 256-cycle wrap to resynchronize -- unrelated to the `core`-tagged
tasks' properties, which are genuinely 1-2 cycle local and close at step 5.)

Full numbers (including the exact `sby` transcripts) are in `.superpowers/sdd/task-9-report.md`.
