# Tiny Tapeout project information
project:
  title:        "Protocol Emulator"      # Project title
  author:       "anbaghel"      # Your name
  discord:      ""      # Your discord username, for communication and automatically assigning you a Tapeout role (optional)
  description:  "Programmable protocol-emulator CPU (UART/SPI/I2C in firmware); warm-up: fixed UART TX"      # One line description of what your project does
  language:     "Verilog" # other examples include SystemVerilog, Amaranth, VHDL, etc
  clock_hz:     50000000       # Clock frequency in Hz (or 0 if not applicable)

  # How many tiles your design occupies? A single tile is about 167x108 uM.
  tiles: "6x4"          # Valid values: 1x1, 1x2, 2x2, 3x2, 4x2, 6x2 or 8x2

  # Your top module name must start with "tt_um_". Make it unique by including your github username:
  top_module:  "tt_um_abagel_coder_protocol_emulator"

  # List your project's source files here.
  # Source files must be in ./src and you must list each source file separately, one per line.
  # Don't forget to also update `PROJECT_SOURCES` in test/Makefile.
  source_files:
    - "project.v"
    - "pe_timebase.v"
    - "pe_gpio.v"
    - "pe_imem_ff.v"
    - "pe_fifo.v"
    - "pe_core.v"
    - "pe_host_spi.v"

# The pinout of your project. Leave unused pins blank. DO NOT delete or add any pins.
# This section is for the datasheet/website. Use descriptive names (e.g., RX, TX, MOSI, SCL, SEG_A, etc.).
pinout:
  # Inputs
  ui[0]: "PIN_IN0"
  ui[1]: "PIN_IN1"
  ui[2]: "PIN_IN2"
  ui[3]: "PIN_IN3"
  ui[4]: "HOST_SCK"
  ui[5]: "HOST_MOSI"
  ui[6]: "HOST_CS_N"
  ui[7]: "RUN"

  # Outputs
  uo[0]: "PIN_OUT0"
  uo[1]: "PIN_OUT1"
  uo[2]: "PIN_OUT2"
  uo[3]: "PIN_OUT3"
  uo[4]: "PIN_OUT4"
  uo[5]: "PIN_OUT5"
  uo[6]: "PIN_OUT6"
  uo[7]: "HOST_MISO"

  # Bidirectional pins
  uio[0]: "PIN_IO0"
  uio[1]: "PIN_IO1"
  uio[2]: "PIN_IO2"
  uio[3]: "PIN_IO3"
  uio[4]: "PIN_IO4"
  uio[5]: "PIN_IO5"
  uio[6]: "PIN_IO6"
  uio[7]: "PIN_IO7"

# Do not change!
yaml_version: 6
